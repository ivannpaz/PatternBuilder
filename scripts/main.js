/**
 * Build everything here
 * @return void
 */
$(document).ready(function(){
    $('#artboard > canvas').patternBuilder({
        frameSize       : 40,
        frameColor      : '#24282F',
        background      : '#FFFFFF'
    });
});
